# Start Terraform ##############################################################
# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info
.terraform.lock.hcl

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# IDE
.vscode/
.idea/
*.iws
*.iml
*.ipr
**/*.swp
**/*.swo
**/.apt_generated
**/.classpath
**/.factorypath
**/.project
**/.settings
**/.springBeans
**/.sts4-cache
**/target
**/.mvn/**
!**/src/main/**/target/
!**/src/test/**/target/
**/.gemini
**/.specify

# Zed specific
.zed/

# NetBeans
**/nbproject/private/
**/nbbuild/
**/dist/
**/nbdist/
**/.nb-gradle/
!**/src/main/**/build/
!**/src/test/**/build/

# Mac specific
.DS_Store


# Rust specific ignores
**/target/

# Python specific ignores
*__pycache__
.prettierrc

# Go workspace file
go.work


# MCP servers
sample-services/mcp-servers/repairshop/.venv/
sample-services/mcp-servers/remote-lock/.venv/

# Ai Assistant
.venv
sample-services/ai_assistant/.env
sample-services/ai_assistant/agent/__pycache__
sample-services/ai_assistant/__pycache__

# Data API
base-services/data-api/api/

# VHAL Simulator
sample-services/vhal-simulator/telemetry

# Predictive Maintenance
sample-services/predictive-battery-maintenance/api

# Python Client
sample-clients/python/certificates
sample-clients/python/proto
sample-clients/python/nexus_client_config.json
base-services/registration/pki/factory-ca/data

#Binaries of the example clients
sample-clients/vehicle-client/vehicle-client
sample-clients/telemetry-subscriber/telemetry-subscriber

#Generated protobuf files
**/*.pb.go


# PKI and Certificate Security ##################################################
# Exclude all certificate and key files (various formats)
# These should be generated when running the bootstrapping script or with the cert
# generation scripts.
*.pem
*.key
*.key.pem
*.crt
*.crt.pem
*.csr
*.csr.pem
*.p12
*.pfx
*.jks
*.der
**/pki/**/data/
**/pki/**/*.conf
**/certificates/



# Exclude environment variable file with bootstrapping default. It should be either created when
# running the bootstrapping script or stored elsewhere
**/.bootstrap_env*
/sample-services/ai_assistant/uv.lock
**/*.srl
**/*.backup
**/*.ext

# gh token for release
set-gh-token-for-release-dae.sh
